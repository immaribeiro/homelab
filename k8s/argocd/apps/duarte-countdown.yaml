apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: duarte-countdown
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/immaribeiro/homelab.git
    targetRevision: HEAD
    path: k8s/manifests/duarte-countdown.yml
  destination:
    server: https://kubernetes.default.svc
    namespace: duarte-countdown
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
